import { AmmanConfig } from '../types';
import { AmmanState, ValidatorConfig } from './types';
/**
 * @private
 */
export declare const DEFAULT_VALIDATOR_CONFIG: ValidatorConfig;
/**
 * @private
 */
export declare function initValidator(config: Required<AmmanConfig>, forceClone?: boolean): Promise<AmmanState>;
