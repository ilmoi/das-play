import { Keypair, PublicKey } from '@solana/web3.js';
/**
 * Represents anything that can be used to extract the base58 representation
 * of a public key.
 * @private
 */
export declare type KeyLike = string | PublicKey | Keypair;
export declare function isKeyLike(val: any): val is KeyLike;
export declare function publicKeyString(key: KeyLike): string;
